alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ cmp file.txt file1.txt
file.txt file1.txt differ: byte 5, line 3
Утилита осуществляет побайтовое сравнение содержимого двух указанных файлов. Выводит номера отличающихся строки и байта.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ comm file.txt file1.txt
		1
		2
3
4
5
	6
	7
	8
	9
comm: file 2 is not in sorted order
	0
Команда для сравнения двух текстовых файлов с отсортированными по алфавиту строками. На выход файлы поступают построчно в два столбца с обобщенными одинаковыми строками.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ wc -L file3.txt
20 file3.txt
Утилита выводит длину наибольшей строки в полученном файле.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ tail -n 5 file1.txt
6
7
8
9
0
Команда для вывода пяти последних строк файла

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ ls | tee -i file4.txt
file1.txt
file3.txt
file4.txt
file.txt
Untitled Document 5
Команда выводит на экран, или же перенаправляет выходной материал команды и копирует его в файл или в переменную.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ cat file5.txt | tr 'a' 'b'
bbbbbbbbbbbbbbbbbb
Команда служит для перевода (замены) выбранных символов в другие символы или удаления их.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ echo -e aaaababav | uniq -c
      1 aaaababav
Утилита uniq предназначена для поиска одинаковых строк в полученном тексте. 

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ od -b file5.txt
0000000 141 141 141 141 142 142 142 142 142 141 142 141 142 141 142 141
0000020 141 142 012
0000023
Данная команда преобразует содержимое файла в различные форматы, например, в восьмеричный. 

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ sum file5.txt
21967     1
Команда вычисляет и выводит 16-битную контрольную сумму для указанного файла; кроме того, выдается количество блоков в файле.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ echo "ABCDEFG" | cut -b 1
A
Команда cut вырезает из поступившего текста байт под номером 1

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ mc
Запуск GNU Midnight Commander – файлового менеджера с текстовым интерфейсом

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ head -c 3 file1.txt
1
2
Утилита позволяет просматривать начальные несколько строк файла.

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ find . -type f | sed 'p;s:file:example:g' | xargs -n4 -p mv
mv ./file.txt ./example.txt ./file5.txt ./example5.txt ?...
mv ./xac ./xac ./file1.txt ./example1.txt ?...
mv ./xae ./xae ./xaa ./xaa ?...
mv ./file4.txt ./example4.txt ./xaf ./xaf ?...
mv ./file6.txt ./example6.txt ./file3.txt ./example3.txt ?...
mv ./xab ./xab ./xad ./xad ?...
mv ?...
Команды находят файлы, после чего меняет элементы в именах этих файлов на другие, после чего организует вывод всех файлов в 4 столбца

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20$ split file5.txt –n 4
Данная утилита с флагом –n разбивает файл на 4 других файлов

alex@alex-HP-Laptop-15-dw0xxx:~/ForLab20/lab20$ touch file7 file8 total b8b|echo "12345678">file7|head -c 4 file7|tee -i file8|cat file7 file8 >> total| od -b total|tee -i b8b|find -name "file*"|xargs rm -rf|find -name "xa*"|xargs rm -rf
Создаем файлы для работы, потом записываем число в file7. Получаем первые 4 знака из file7, а результат записываем в file8. Объединяем file6 и file7 и записываем результат в total. Переводим содержимое файла total в восьмиричный форма и записываем результат в файл b8b. После удалеям все файлы, начинающиеся на file и на xa.




